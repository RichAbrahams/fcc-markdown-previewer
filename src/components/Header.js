import React, { Component } from "react";

export default class Header extends Component {
  render() {
    return (
      <nav className="header lime darken-3">
         <h3 className="left brand-logo">GitHub Markdown Previewer</h3>
      </nav>
    );
  }
}
